import { Navigation } from './navigation'

/**
 * Navigation wrapper - the Navigation component itself will handle 
 * conditional rendering based on the current route.
 */
export async function ConditionalNavigation() {
  return <Navigation />
} 